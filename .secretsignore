# Test fixtures are sample real-world config files (e.g. a gitconfig with a
# placeholder signing key); they contain no real secrets. Exclude them from
# ripsecrets scanning to avoid false positives.
tests/fixtures/
